@model WebApplicationDiplom.ViewModels.ReserveOfPersonnelViewModel
@{
    ViewBag.Title = "Перевод работника из резерва";
}

<form asp-action="Edit" asp-controller="ReserveOfPersonnel" asp-route-id="@Model.ReserveId">
    <div asp-validation-summary="ModelOnly"></div>
    <input type="hidden" asp-for="ReserveId" class="form-control" />
    @*@Html.DisplayFor(model => model.Position.JobTitle)*@
    </div>
    <br />
    <div>
      
   <label>Вывести из резерва</label>
    </div>
 
    <div class="form-group">
        <input type="submit" value="Да" class="btn btn-default" />
    </div>

</form>